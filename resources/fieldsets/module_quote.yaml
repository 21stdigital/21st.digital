title: 'Module Quote'
fields:
  -
    handle: quote
    field:
      type: textarea
      display: Quote
      validate:
        - required
  -
    handle: testimonial
    field:
      max_items: 1
      mode: select
      create: false
      collections:
        - client_representatives
      type: entries
      display: Testimonial
      duplicate: false
      validate:
        - required
