# Set default behavior for all text files
# Normalize line endings to LF for consistent cross-platform compatibility
*               text=auto eol=lf

# Define custom diff algorithms for specific file types to improve diff readability
# Using language-appropriate diff drivers enhances clarity when reviewing changes
*.blade.php     diff=html
*.css           diff=css
*.html          diff=html
*.md            diff=markdown
*.php           diff=php
*.js            diff=javascript
*.json          diff=json

# Exclude specific files and directories from being exported
# Prevent inclusion of development and configuration files in exported archives
/.github/       export-ignore
CHANGELOG.md    export-ignore
.editorconfig   export-ignore
